---
interface Props {
  no: string;
  label: string;
  hint?: string;
}
const { no, label, hint } = Astro.props;
---

<div class="sec-rule reveal">
  <span class="sec-no"><b>No. {no}</b> — {label}</span>
  {hint && <span class="sec-hint">{hint}</span>}
</div>
