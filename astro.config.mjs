// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// User site served at the domain root  no base path needed.
export default defineConfig({
  site: 'https://fadihachani.github.io',
});
